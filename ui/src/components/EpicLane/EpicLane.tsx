// EpicLane component — used in the swimlane view (future)
// For now, the board uses a simple column view.
// This component will be used when swimlane mode is implemented.

export function EpicLane() {
  return null;
}
